# arduino-servo-project
This project presents a system that controls a servo motor using an ultrasonic sensor and an Arduino board. The servo rotates to a specified angle when an object is detected within 10 cm and returns to its initial position when the object moves away.
